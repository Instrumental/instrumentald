source "https://rubygems.org/"

gem "instrumental_agent", ">=0.13.2"
gem "pidly", ">=0.1.3"
gem "toml", "~>0.0.3"

group :development do
  gem "rake", ">=0"
  gem "fpm", ">=1.3.3"
  gem "package_cloud", ">=0"
  gem "test-kitchen", ">=0"
  gem "kitchen-vagrant", ">=0"
  gem "kitchen-puppet", ">=0"
  gem "berkshelf", ">=0"
  gem "librarian-puppet", ">=0"
  gem "puppet", ">=0"
  gem "serverspec", ">=0"
end
